-- =============================================
-- ID CARD / PASSPORT NUMBER (ตรวจแบล็คลิสต์ 2 ชั้น — ชื่อ + เลขบัตร)
-- กันเคสโจรเปลี่ยนชื่อ (เลขบัตรเดิม) และกันเคสลูกค้าจริงชื่อซ้ำโจร (คนละเลขบัตร)
-- =============================================

-- เลขบัตรที่ผูกกับรายชื่อในบัญชีดำ (13 หลักบัตรประชาชนไทย หรือเลขพาสปอร์ตชาวต่างชาติ)
ALTER TABLE blacklist ADD COLUMN IF NOT EXISTS id_card_number TEXT;

-- เก็บเลขบัตรของลูกค้าไว้ในประวัติ เพื่อให้ตรวจแบล็คลิสต์ครั้งต่อไปแม่นขึ้น แม้ชื่อ/เบอร์จะเปลี่ยน
ALTER TABLE customers ADD COLUMN IF NOT EXISTS id_card_number TEXT;
