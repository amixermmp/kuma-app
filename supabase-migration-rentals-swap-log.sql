-- =============================================
-- RENTALS SWAP LOG (รายวัน) — ตอนนี้มีแค่ monthly_rentals ที่เก็บประวัติสลับรถ
-- rentals (รายวัน) ไม่มีคอลัมน์นี้เลย ทำให้ย้อนดูไม่ได้ว่าคันไหนใช้จริงตอนไหน
-- ถ้าเคยสลับรถระหว่างเช่า (เช่นรถเสีย) — ใช้แก้ให้รายงานรายได้แยกรายคันถูกต้อง
-- =============================================

ALTER TABLE rentals ADD COLUMN IF NOT EXISTS swap_log JSONB DEFAULT '[]';
