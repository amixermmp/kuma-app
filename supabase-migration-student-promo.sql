-- =============================================
-- STUDENT PROMO FLAG (ต่อปุ่ม "ราคานักศึกษา" เข้ากับระบบโปรโมชั่นที่มีอยู่แล้ว)
-- ก่อนหน้านี้ปุ่มราคานักศึกษาลด -50 บาท/วันตายตัวทุกรุ่น ไม่เคยเชื่อมกับ eligible_bike_ids
-- ตอนนี้ owner เลือกได้เองว่าโปรโมชั่นไหนคือ "ราคานักศึกษา" และรุ่นไหนร่วมรายการบ้าง
-- =============================================

-- true ได้แค่แถวเดียวเท่านั้น (บังคับที่ชั้น API ตอน PUT/POST)
ALTER TABLE promotions ADD COLUMN IF NOT EXISTS is_student_promo BOOLEAN DEFAULT FALSE;
